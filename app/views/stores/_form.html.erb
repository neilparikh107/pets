<%= simple_form_for(@store) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :suburb %>
    <%= f.input :manager %>
    <%= f.input :food %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
